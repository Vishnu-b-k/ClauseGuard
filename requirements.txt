# --- Needed to run this scaffold today (mocked ADK/Qdrant/Lyzr) ---
python-docx>=1.1
pypdf>=4.0
pydantic>=2.6
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9

# --- Needed for the NEXT sprint slice: swapping mocks for real services ---
python-dotenv>=1.0.0
qdrant-client>=1.9          # src/retrieval/qdrant_client.py -> real client
fastembed>=0.2              # dense embeddings for Qdrant hybrid search
google-genai>=0.2.0         # Google ADK / Gemini SDK
tenacity>=8.2               # NFR-204: circuit breakers / backoff on LLM calls

